Let's call files here like

_node*_NODEBUNDLE.scss

These files contains of all node displays.
We can split this files into node displays but I think it's not so important.

See folder contents for examples.